{
  "name": "socketchat-frontend",
  "version": "1.0.0",
  "description": "SocketChat - Frontend Web Application",
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "vite build",
    "preview": "vite preview --port 3000",
    "start": "vite preview --port 3000 --host"
  },
  "dependencies": {
    "vite": "^5.4.14"
  }
}
